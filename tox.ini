[tox]
envlist = py27,coverage

[testenv:devenv]
envdir = devenv
basepython = python2.7
usedevelop = True
deps =
    -rrequirements.txt
    coverage>=4.0
    pdbpp
    flake8
commands =
    flake8 .
    coverage erase
    coverage run --append -m py.test --blockchain-type=tester --exitfirst ./raiden/tests/smart_contracts {posargs}
    coverage run --append -m py.test --blockchain-type=tester --exitfirst ./raiden/tests/integration {posargs}
    coverage run --append -m py.test --blockchain-type=tester --exitfirst ./raiden/tests/unit {posargs}

[testenv]
deps =
    -rrequirements.txt
    flake8
    coverage>=4.0
commands =
    flake8 .
    coverage erase
    coverage run --append -m py.test --exitfirst ./raiden/tests/smart_contracts {posargs}
    coverage run --append -m py.test --exitfirst ./raiden/tests/integration {posargs}
    coverage run --append -m py.test --exitfirst ./raiden/tests/unit {posargs}
