import { AllowedDecimalsDirective } from './allowed-decimals.directive';

describe('AllowedDecimalsDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowedDecimalsDirective();
    expect(directive).toBeTruthy();
  });
});
